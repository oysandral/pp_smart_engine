Smart recommendation engine 🇺🇸/🇵🇱

Status: under development (Work in Progress)ю

pp_smart_engine is a project designed to improve and personalize e-commerce search using vectorization. Instead of basic keyword matching, the engine uses embeddings to understand the semantic meaning of products and user queries, allowing it to find relevant recommendations based on similarity in vector space.

Current implementation details:
- dockerized environment with PostgreSQL integration;
- integration with Google Gemini AI;
- REST API endpoints with full CRUD functionality;
- data validation powered by Pydantic.

Status: w trakcie realizacji (Work in Progress).
 
pp_smart_engine to system stworzony w celu ulepszenia i personalizacji wyszukiwania w e-commerce za pomocą wektoryzacji. Zamiast prostego dopasowywania słów kluczowych, silnik wykorzystuje embeddingi, aby zrozumieć znaczenie semantyczne produktów i zapytań użytkowników. Dzięki temu system znajduje trafne rekomendacje na podstawie podobieństwa w przestrzeni wektorowej.

Status implementacji:
- środowisko oparte na Dockerze z integracją PostgreSQL;
- integracja z Google Gemini AI;
- endpointy REST API z pełną funkcjonalnością CRUD;
- walidacja danych przy użyciu Pydantic.