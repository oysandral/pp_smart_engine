version: '3.8'

services:
  db:
    image: ankane/pgvector
    environment:
      POSTGRES_USER: admin_user
      POSTGRES_PASSWORD: 1234password
      POSTGRES_DB: smart_rec_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: